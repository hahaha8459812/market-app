# 32 位随机串（示例）：openssl rand -hex 16
JWT_SECRET=change-me-change-me-change-me-change-me

# Postgres（容器内自带）
POSTGRES_USER=market_user
POSTGRES_PASSWORD=market_pass
POSTGRES_DB=market_db

# 可选：Prisma schema 变更导致启动循环时，自动清 public schema（会丢数据）
MARKET_RESET_DB_ON_SCHEMA_ERROR=false
