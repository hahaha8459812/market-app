version: "3.9"

services:
  market-app:
    build: .
    container_name: market-app
    restart: always
    ports:
      - "8080:3000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-me}
      - POSTGRES_USER=${POSTGRES_USER:-market_user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-market_pass}
      - POSTGRES_DB=${POSTGRES_DB:-market_db}
    volumes:
      - ./data:/data
